# Reference Archive

`references/archive/` 保存已经退出 active stable path、但仍值得在仓库内保留的少量历史 volume。

这些文件仍然可追溯，但默认不再承担当前 release governance、runtime contract 或 operator navigation 的 live truth。

## Start Here

- [`changelog/pre-v2.3.53.md`](./changelog/pre-v2.3.53.md): 当前仍保留的历史 changelog volume

## Rules

- 当前稳定路径仍优先保留在原位，例如 `references/changelog.md`。
- archive volume 用来承接仍需 repo 内可见性的少量历史长尾，不把 live stable path 改成漂移路径。
- 已经完全退出活跃消费且只剩说明价值的 proof details，默认交给 git history，而不是继续占用 archive surface。
