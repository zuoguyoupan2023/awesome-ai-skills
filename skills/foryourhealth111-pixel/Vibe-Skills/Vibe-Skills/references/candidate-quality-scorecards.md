# Candidate Quality Scorecards

The board scorecards below summarize the minimum evidence expected before Stage B consumes a candidate track.

| Candidate | Evidence focus | Required carry-forward note |
|---|---|---|
| `memory-quality-eval` | owner boundary + fallback integrity | keep `candidate_shadow` until canonical owners are untouched |
| `openworld-runtime-eval` | replay + confirm discipline | keep candidate-only until takeover risk stays zero |
| `document-failure-taxonomy` | degraded mode + provenance | no-go if `failure_object` contract drifts |
| `prompt-intelligence-eval` | advisory safety + provenance | no-go if route override appears or a second prompt router is implied |

This board must remain subordinate to promotion board v2.
