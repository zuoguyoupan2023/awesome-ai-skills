# OpenClaw Runtime-Core Preview Candidate

This bundle is a **candidate scaffold**, not a promotion proof.

It exists to freeze the evidence references required before OpenClaw can be
described above `preview`.

Current truth:

- lane: `openclaw/runtime-core-preview`
- status: `preview`
- closure: `runtime-core-preview`
- promotion: blocked until fresh-machine proof and replay/docs synchronization exist

This directory must not be used to claim:

- `supported-with-constraints`
- `full-authoritative`
- host-native plugin closure
- cross-platform parity
